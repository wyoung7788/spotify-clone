/** @type {import('next').NextConfig} */
const nextConfig = {
    /*
    webpack(config){
        config.experiments = {... config.experiements, topLevelAwait:true};
        return config;
    }
};*/}

export default nextConfig;
